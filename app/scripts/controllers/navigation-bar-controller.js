'use strict';

angular.module('evalu8Demo')
  .controller('NavigationBarController', ['$scope', '$rootScope', function ($scope, $rootScope) {
      //$scope.userName = $rootScope.globals.currentUser.username;
  }]);
